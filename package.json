{
  "name": "mars-rover-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "tsc -w",
    "develop": "rm -rf dist && tsc && nodemon dist/app.js",
    "start": "rm -rf dist && tsc && nodemon dist/app.js",
    "test": "jest --no-cache --watchAll --detectOpenHandles"
  },
  "author": "Eugene Peter Maestrado",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.14",
    "ts-node": "^10.4.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.4.4"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.11.6",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7"
  }
}
